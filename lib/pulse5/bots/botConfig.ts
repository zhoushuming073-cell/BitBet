export const BOT_ALPHA_CONFIG = {
  windowMs: 36_000,
  recentMs: 9_000,
  earliestProgress: 0.12,
  latestProgress: 0.7,
  timingJitter: 0.035,
  minCumulativeReturn: 0.00015,
  minTickBias: 0.53,
  minMeanDeviation: 0.00005,
  minConfidence: 0.54,
  confidenceJitter: 0.055,
  baseRisk: 0.04,
  strongRisk: 0.055,
  maxRisk: 0.07,
  lossRiskReduction: 0.015,
  skipRoundChance: 0.08,
  /** Multiple entries are allowed, with time for a genuinely new signal. */
  orderCooldownMs: 24_000,
} as const;

export const BOT_BETA_CONFIG = {
  windowMs: 28_000,
  recentMs: 8_000,
  earliestProgress: 0.2,
  latestProgress: 0.66,
  timingJitter: 0.04,
  minCumulativeReturn: 0.00035,
  minMeanDeviation: 0.0002,
  minConfidence: 0.6,
  confidenceJitter: 0.06,
  baseRisk: 0.03,
  strongRisk: 0.045,
  maxRisk: 0.05,
  lossRiskReduction: 0.01,
  skipRoundChance: 0.22,
  strongTrendTickRatio: 0.76,
  orderCooldownMs: 36_000,
} as const;
